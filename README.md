# json2css
Json to css
Converts json data to style information.

```
var jdata = {
        ".box1" : { 
            border: "2px solid red"            
        }
    };    
    
$("head").json2css(jdata);
```
